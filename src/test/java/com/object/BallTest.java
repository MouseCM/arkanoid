package com.object;

import org.junit.jupiter.api.Test;

public class BallTest {
    @Test
    public void testBallMovement() {
        // Ball ball = new Ball( 10.0, 100.0, 3.0, -3.0, 10.0, 5.0, 45.0);
        // ball.update();
        // assertEquals(100, ball.getX());
        // assertEquals(100, ball.getY());

    }
}
